{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "watchPatterns": ["**"]
  },
  "deploy": {
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port $PORT --timeout-graceful-shutdown 25",
    "healthcheckPath": "/ready",
    "healthcheckTimeout": 30
  }
}
