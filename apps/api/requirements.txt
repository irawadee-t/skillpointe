# Core
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic>=2.11.7
pydantic-settings>=2.7.0
email-validator==2.3.0

# Supabase
supabase==2.31.0
python-jose[cryptography]==3.5.0

# Redis
redis==5.2.1

# Scheduler (6-hour match recompute)
apscheduler>=3.10.0

# HTTP
httpx==0.28.1

# HTML parsing — career-source scraping (app/skilled_pro/career_sources.py)
# and every packages/scraper adapter import BeautifulSoup directly.
beautifulsoup4>=4.12.0

# LLM extraction (Phase 7)
openai>=1.0.0

# Database (Phase 3+)
asyncpg==0.31.0

# Sentry (observability)
sentry-sdk[fastapi]==2.66.1

# Import pipeline / ETL (Phase 4+)
pandas>=2.2.0
openpyxl>=3.1.0
python-dateutil>=2.9.0
psycopg2-binary>=2.9.9

# Dev / test
pytest==8.3.4
pytest-asyncio==0.25.2
python-dotenv==1.2.2

# SKILLED Pro résumé PDF
fpdf2==2.8.7

# Resume upload + parse (Phase 9+)
pypdf>=5.0.0
python-docx>=1.1.0
python-multipart>=0.0.9
